/**
 * luacheck-ts — a TypeScript port of the luacheck Lua linter.
 *
 * This is the public entry point of the package. It is a placeholder for
 * now; the real analysis API lands in later phases of the port.
 */

/**
 * Placeholder entrypoint.
 *
 * TODO(port): replace with the real luacheck API surface once the port
 * lands in later phases.
 */
export function luacheck(): string {
  return "luacheck-ts";
}
